# Environment Configuration
# Copy this file to .env and fill in your actual values

# Server Configuration
PORT=3000
NODE_ENV=development

# Email Configuration
# For Gmail: Use "gmail" as service
# For other providers: Specify SMTP settings
EMAIL_SERVICE=gmail
EMAIL_USER=your-email@gmail.com
EMAIL_PASSWORD=your-app-specific-password

# Contact Form Recipient
CONTACT_EMAIL=info@studentorg.edu

# Database Configuration (if using MongoDB)
# MONGODB_URI=mongodb://localhost:27017/student-org
# DB_NAME=student_organization

# Database Configuration (if using MySQL)
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=your-password
# DB_NAME=student_org

# JWT Secret (for authentication if needed)
# JWT_SECRET=your-secret-key-here

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:3000

# Notes:
# 1. For Gmail, you need to enable "Less secure app access" or use App Passwords
# 2. To create Gmail App Password:
#    - Go to Google Account settings
#    - Security > 2-Step Verification > App passwords
#    - Generate a new app password for "Mail"
#    - Use that password in EMAIL_PASSWORD
